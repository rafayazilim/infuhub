/** Platform hizmet komisyonu — çekim tutarı üzerinden kesilir (brüt × oran). */
export const PAYOUT_PLATFORM_COMMISSION_RATE = 0.1;

/** Minimum çekim talebi (TL). */
export const PAYOUT_MIN_WITHDRAWAL_GROSS = 100;
